# דוח Gate 1 — Knowledge Flow (2026-08-05)

> משימה: "עצור פיתוחים חדשים. סגור את Gate 1 — חבר Drive Sensor, פרסר robust-core, צור פרקים קנוניים, הרץ 3 שאילתות בדיקה עם Evidence/Provenance/Replay."
> סביבת ביצוע: סשן מרוחק (sandbox) עם גישת Git + Google Drive MCP; ללא egress ל-vercel.app/supabase.co.

---

## הכרעה

## ❌ Gate 1 לא נסגר.

הידע עדיין לא זורם לתוך ה-RAG ואי-אפשר היה להריץ את שאילתות הבדיקה מהסביבה הזו. מה שכן — **כל העבודה שההיתה אפשרית מרחוק בוצעה**, והמרחק לסגירה צומצם לשלוש פעולות שרק אתה יכול לבצע (מפורטות בסוף).

---

## 1. מה נטען

| יעד | תוצאה |
|---|---|
| קורפוס RAG | **0 קבצים נטענו** — ה-backend (`matriya-back.vercel.app`) חסום מהסביבה (proxy 403), ואין credentials בסשן |
| Drive — סט קנוני | תיקייה `MATRIYA_Canonical_Chapters_2026-08-05` נוצרה + מסמך INDEX הועלה (🟢 בוצע) |
| רפו — סט קנוני | **14 קבצים** ב-`MATRIYA/Corpus_Canonical/chapters/` עם MD5 לכל קובץ (🟢 בוצע) |

## 2. כמה קבצים ואילו נכשלו

- התקבלו 16 קבצים → **14 קנוניים**. שניים הוסרו כ**כפילויות זהות בייט-לבייט** (אומת ב-MD5): ‏Chapter231 (=Chapter23), ‏Chapter261 (=Chapter26). ‏Chapter171 שוּנה שמו ל-Chapter17 (ארטיפקט שם קובץ בלבד).
- **נכשל/נחסם:** העתקת ה-docx הבינאריים ל-Drive מתוך הסשן (מגבלת ערוץ ה-MCP לקבצים בינאריים בנפח הזה) — נותר צעד גרירה אחד; פרסור robust-core ל-E3 (חסום — backend לא נגיש).
- **robust-core:** ‏6 הנכסים הנקובים אומתו קיימים ב-Drive (הפורמולציות 04-05/04-26, מרשמים INTUMESCENT, דוח 25IC0391, ‏NHL 3.5, שוק העלייה 27) כולל file IDs. **הרשימה המלאה של ה-13 אינה שמורה ברפו** — צריך לשחזר מארטיפקט האודיט v0.2 או להריץ מחדש.

## 3. שלוש שאילתות הבדיקה

**לא הורצו.** ‏`POST /ask-matriya` דורש (א) egress ל-backend — חסום ב-proxy מהסביבה הזו (נבדק היום: ‏CONNECT 403, זהה ל-2026-07-04), (ב) טוקן התחברות — אין בסשן. אין תוצאות מפוברקות לדווח.

## 4. הוכחת Provenance

- לסט הקנוני: MD5 לכל קובץ + מקור (upload מ-2026-08-05) + החלטות דילול — ב-`Corpus_Canonical/INDEX.md` וגם באינדקס שב-Drive.
- לנכסי robust-core: אומתו מול Drive API עם file IDs (למשל `1WI0_OYBFudfFxcKQAtu1VYUfoQH1YpXe` = דוח 25IC0391).
- Provenance של תשובות מה-RAG — לא ניתן להוכיח עד שהשאילתות ירוצו.

## 5. הוכחת Replay

**לא בוצעה** — תלויה בהרצת השאילתות. נשארת כחלק מקריטריון הסגירה (אותה שאילתה × 2 ⇒ אותה תשובה ואותם מקורות).

## 6. מצב החיישן — הממצא המרכזי של היום

החיישן **אינו מנותק ברמת הקוד**: ה-workflow רץ מהענף הראשי כל ~4 שעות ורושם heartbeat (‏212 פעימות; אחרונה 2026-08-05 17:55 UTC). כל פעימה מסתיימת `not_configured` מסיבה אחת בלבד: **שלושת ה-Secrets של Google OAuth חסרים ברפו**. אין שום שינוי קוד נדרש — ברגע שהסודות נוספים, החיישן חי מהריצה הבאה.

## 7. מדדי המצב (לפי LAW-EVIDENCE-001 — לא עודכנו מעבר לראיות)

| מדד | לפני | אחרי | הערה |
|---|---|---|---|
| ingestion coverage | 0% | **0%** (ללא שינוי) | שום דבר לא נכנס ל-RAG בפועל |
| KUR | 0 | **0** (ללא שינוי) | |
| Drive Sensor | Disconnected | **Disconnected**, אבל אומת: חוסם יחיד = secrets | ‏P-003 צומצם לפעולה אחת |
| Drive corpus access | read-only | **read+write אומת** (סשן MCP) | תיקייה+אינדקס נוצרו |
| Priority | ABSENT | **ABSENT** — נרשם D-006: לא ישתנה עד אינטגרציה פעילה ומאומתת | לפי הנחייתך |

## 8. שלוש הפעולות שנותרו לסגירת Gate 1 (רק אתה יכול)

1. **הוסף 3 Secrets לרפו** (Settings → Secrets → Actions): ‏`GOOGLE_CLIENT_ID`, ‏`GOOGLE_CLIENT_SECRET`, ‏`GOOGLE_REFRESH_TOKEN` (הוראות מלאות: `docs/DRIVE_INTAKE_ACTION.md`). תוך ≤4 שעות החיישן חי.
2. **גרור את 14 הקבצים** מ-`MATRIYA/Corpus_Canonical/chapters/` לתיקיית Drive ‏`MATRIYA_Canonical_Chapters_2026-08-05`, והרץ ingestion ל-robust-core + לפרקים דרך המערכת (UploadTab / ‏`/ingest/file`) מסביבה עם גישה ל-backend.
3. **הרץ את 3 שאילתות הבדיקה** (מוצעות: "מה הרכב הפורמולציה בניסוי 04-26?", "מה תוצאות דוח 25IC0391?", "מה קובע פרק 30 לגבי System of Record?") — פעמיים כל אחת, ותעד: מסמכים שנמצאו, Evidence, מקור+Provenance, זהות Replay. עדכן אז את knowledge.json ואת הדוח הזה ל-✅.
